syntax = "proto3";

package at.tugraz.oop2;

message MapRequest {
    string address = 1;
}

message MapResponse {
    string map_image_url = 2;
}

service MapService {
    rpc GetMap(MapRequest) returns (MapResponse);
}
