syntax = "proto3";

package at.tugraz.oop2;

message RoadByIdRequest {
    int id = 0;
}

message RoadByIdResponse {
    Road road;
}

service MapService {
    rpc GetRoadById(RoadByIdRequest) return (RoadByIdResponse);
}
